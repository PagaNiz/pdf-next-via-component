import { StyleSheet } from "@react-pdf/renderer";

export const styles = StyleSheet.create({
    title: {
        border: '2px solid #222',
        marginBottom: '25px',
        paddingLeft: '5px'
    }
})